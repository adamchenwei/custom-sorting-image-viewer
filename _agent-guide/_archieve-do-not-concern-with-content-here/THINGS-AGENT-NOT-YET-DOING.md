1. creating a git repo base on the app name for the user automatically and connect it to the folder before anything
2. somehow create all the database and keys for the user automatically before anything
